<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">
    <t t-name="dynamic_dashboards.PreviewWidget" owl="1">
        <div class="o_dashboard_item_preview w-100" style="height: 400px; padding: 15px; border: 2px dashed #e0e0e0; border-radius: 15px; background: #f8f9fa;">
            <t t-if="!props.record.data.model_name or !props.record.data.group_by_field_name">
                <div class="d-flex flex-column h-100 align-items-center justify-content-center text-muted">
                    <i class="fa fa-magic fa-4x mb-3 text-secondary opacity-50"></i>
                    <h4 class="text-secondary opacity-75">Live Preview</h4>
                    <span class="opacity-75">Select a Model and Group By field to see the magic.</span>
                </div>
            </t>
            <t t-else="">
                <t t-if="state.loading">
                    <div class="d-flex h-100 align-items-center justify-content-center">
                        <i class="fa fa-spinner fa-spin fa-3x text-primary"></i>
                    </div>
                </t>
                <t t-else="">
                    <ChartWidget 
                        t-key="props.record.data.chart_type + '_' + (state.data.labels ? state.data.labels.length : 0)"
                        item="{
                            id: props.record.data.id || 0,
                            name: props.record.data.name || 'Preview',
                            chart_type: props.record.data.chart_type,
                            col_span: '12'
                        }"
                        previewData="state.data"
                        darkMode="false"
                        dateFilter="'all'"
                    />
                </t>
            </t>
        </div>
    </t>
</templates>
